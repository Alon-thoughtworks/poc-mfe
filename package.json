{
  "name": "poc-mfe",
  "workspaces": {
    "packages": [
      "booking",
      "container",
      "customer-service",
      "marketing",
      "loyalty",
      "shared-components"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}